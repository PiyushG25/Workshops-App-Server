/*const home = (req , res) => {
    res.write("HOME, ");
    res.end("That's it");
};

const postMessage = (req, res) => {
    console.log(req.body);
    res.end('I have noted your message');
};

module.exports = {
    home, 
    postMessage
};*/